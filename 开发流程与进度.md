# VideoBrowser 开发流程与进度

版本：v1.0  
创建时间：2026-05-25  
关联文档：`开发设计文档.md`  
当前目标：按 MVP 顺序完成一个可运行、可验证的视频增强浏览器基础版本。

---

## 1. 文档用途

本文件作为后续开发入口。每次开始开发前，先查看本文件的“当前开发指针”和“阶段任务表”，再按顺序推进。

`开发设计文档.md` 负责说明产品目标、架构和技术方案；本文件负责说明开发顺序、完成度、验收标准和下一步任务。

---

## 2. 每次开发流程

每次开发必须按以下流程执行：

1. 阅读“当前开发指针”，确认本次应该做哪一个阶段、哪一个任务。
2. 阅读对应阶段的“进入条件”和“验收标准”。
3. 先确认本次代码应归属的模块和包名，禁止把新功能继续堆入单个 Activity 或单个大文件。
4. 只实现当前任务以及它直接依赖的最小改动。
5. 完成后执行对应验证命令或手工验证。
6. 更新本文件中的任务状态、阶段完成度、验证记录和下一步任务。
7. 明确提醒开发工程师进行测试，列出建议测试项。
8. 询问开发工程师是否继续进行下一步开发。
9. 未获得开发工程师确认前，不进入下一任务或下一阶段。
10. 未通过验收时，不进入下一阶段。
11. 修改代码时必须同步补充中文注释，便于开发人员快速判断本次新增或调整了哪些逻辑。

代码规范与模块化要求：

- `MainActivity` 只负责页面控件绑定、用户事件分发、Activity 生命周期转发和轻量 UI 状态更新。
- `WebView` 初始化、浏览器导航、加载状态和生命周期封装到 `browser/`。
- URL、域名、搜索词解析封装到 `utils/`，不要散落在 Activity 内。
- 广告请求拦截封装到 `adblock/`；规则数据结构和匹配逻辑封装到 `rules/`。
- JS 脚本加载和注入封装到 `inject/`；站点识别和站点脚本选择封装到 `site/`。
- 设置读取、默认值和持久化封装到 `settings/`、`storage/`，不要由 UI 层直接承担业务规则。
- AdGuard / EasyList 规则接入必须按阶段拆分：P6 只处理请求级 URL、域名和简单白名单，P7/P8 后再处理 CSS、DOM 和站点限定规则，P11/P12 后再接入规则文件、订阅和复杂语法。
- 每次新增功能时优先创建或扩展对应功能模块，只有 UI 绑定代码可以留在 `MainActivity`。
- 修改已有大文件时，若本次任务涉及独立业务逻辑，应优先迁移到对应模块，再在 UI 层调用。
- 新文件命名应表达单一职责，避免 `Manager` 文件继续膨胀成跨领域集合；跨阶段功能不得混放在同一类中。
- 新增或修改代码时必须添加中文注释；注释应说明新增逻辑的目的、关键业务边界、兼容性处理或非显然判断，避免只重复代码字面含义。
- 阶段完成前必须检查改动范围是否符合模块边界，并在验证记录或备注中说明模块化处理结果。

原生播放器 UI 复用规则（重要）：

- 如果播放器、系统或组件库已经提供现成功能，必须优先调用现有 API、复用原生控件和监听已有状态，只补充缺失能力。
- 手势层只负责控制播放器行为、调用播放器 API 和提供必要 HUD；不得重绘播放器核心 UI，也不得替代原生播放器控件。
- 禁止重复实现播放 / 暂停图标、视频进度条、缓冲进度、Loading 动画、音量系统、亮度层、全屏切换、倍速逻辑、视频时间状态和字幕渲染。
- 播放 / 暂停只能调用播放器 `play()` / `pause()`，状态展示交给原生播放器 UI；不得自定义中央播放按钮或暂停动画。
- 视频进度必须使用播放器原生 seek bar；仅允许增强拖动交互、横向滑动 seek HUD 和快进快退 HUD。
- 缓冲、Loading、时长、当前时间和字幕必须读取播放器原生状态，不允许另维护一套状态或绘制一层同类 UI。
- 音量和亮度必须调用系统能力；允许自定义百分比 HUD，但不允许做假的音量系统或亮度遮罩层。
- 全屏必须使用播放器原生 fullscreen API 或系统方向控制；不得硬改 View Rotation 形成第二套横竖屏状态。
- 倍速必须调用播放器 `playbackSpeed` / `playbackRate`；允许自定义倍速菜单 UI。
- 允许自定义手势识别、HUD 反馈、锁屏按钮、横向滑动 Seek HUD、音量 HUD、亮度 HUD、快进快退动画、倍速菜单 UI、横竖屏按钮 UI。
- 正确架构是 `Gesture Layer -> Player Controller -> Native Player UI`，不是 `Gesture Layer -> Custom UI Overlay -> Native UI`。
- 视频增强目标是增强播放器交互，不是重做播放器；后续不得继续新增或扩展重复的 HTML5 视频浮层控制条。
- 开发前必须检查目标能力是否已有原生实现；完成后在验证记录或备注中说明复用的原生 API、原生控件或必须补充的缺失能力。

强制停点：

- 每完成一个任务，都必须停止自动推进。
- 必须先提醒开发工程师测试当前任务。
- 必须询问开发工程师是否进行下一步。
- 只有开发工程师明确回复继续后，才允许进入下一任务。

---

## 3. 状态标记规则

任务状态使用以下格式：

| 标记 | 含义 |
| --- | --- |
| `[ ]` | 未开始 |
| `[~]` | 进行中 |
| `[x]` | 已完成 |
| `[!]` | 阻塞或存在风险 |
| `[-]` | 延后，不属于当前阶段 |

完成度填写规则：

- 0%：未开始。
- 25%：已完成基础结构，但不可完整验证。
- 50%：核心功能已实现，可局部验证。
- 75%：主要验收项通过，仍有边界问题或测试缺口。
- 100%：阶段验收项全部通过，并已更新验证记录。

---

## 4. 当前开发指针

当前阶段：`G2 规则编译与索引（G2-03 已完成，等待开发工程师测试）`
当前任务：`等待开发工程师测试 G2-03 域名 host / suffix 索引`
下一任务：`G2-04 为 URL contains 规则建立关键词索引`

当前整体完成度：`98%`

判断依据：

- Android 工程结构已存在。
- `MainActivity` 已初始化 `WebView`。
- `AndroidManifest.xml` 已声明 `INTERNET` 权限。
- 已开启 JavaScript、DOM Storage、HTML5 视频免手势播放。
- `gradlew.bat assembleDebug` 已验证通过。
- 已在 `emulator-5554` 安装并启动 App，首页基础 UI 与 WebView 可见。
- P1 主页面 UI 与基础导航已完成。
- 地址栏输入完整 URL、裸域名或关键词时可触发加载。
- 返回、前进、刷新、首页按钮已绑定，后退和前进按钮状态跟随 `WebView.canGoBack()` 和 `WebView.canGoForward()`。
- 已在 `emulator-5554` 启动 App，竖屏 UI dump 显示地址栏、搜索提供方、`WebView` 和底部导航布局正常。
- 已创建 `browser/BrowserManager.kt`，`MainActivity` 已初始化浏览器核心入口。
- `WebView.settings` 基础初始化已迁移到 `BrowserManager.setup()`。
- `load()`、`goBack()`、`goForward()`、`reload()` 基础操作已封装到 `BrowserManager`。
- `onPause()`、`onResume()`、`destroy()` 生命周期入口已封装到 `BrowserManager`。
- P2 范围内的 Cookie 策略、客户端绑定、下载监听、当前 URL、桌面 UA、JS 执行和浏览数据清理已改由 `BrowserManager` 分发；URL 解析、请求拦截、JS 注入和设置存储按后续阶段继续拆分。
- 已在 `emulator-5554` 验证完整 URL、裸域名、关键词搜索、刷新、后退、前进、首页按钮，P1 导航功能未回退。
- 已创建 `utils/UrlUtils.kt`，地址栏输入解析和搜索 URL 生成已从 `MainActivity` 迁移到 `utils/` 模块。
- `UrlUtils` 已覆盖完整 `http://`、`https://`、裸域名、普通文本搜索、空输入和空白输入处理。
- 已添加 `UrlUtils` 本地单元测试。
- `gradlew.bat testDebugUnitTest` 已验证通过。
- `gradlew.bat assembleDebug` 已验证通过。
- 已创建 `browser/ChromeClient.kt`，`WebChromeClient` 的进度、标题和全屏回调入口已从 `MainActivity` 内联对象迁移到 `browser/` 模块。
- `ChromeClient.onShowCustomView()` 已接管全屏视图保存、全屏容器挂载和沉浸式系统 UI 标志设置。
- `ChromeClient.onHideCustomView()` 已接管全屏视图移除、系统 UI 恢复和回调清理，返回键与 Activity 销毁复用同一退出路径。
- 已补齐全屏容器交互属性、Activity 硬件加速和方向配置；HTML5 视频会强制打开原生 controls，避免只能播放但没有暂停、拖动和全屏入口。
- 针对视频测试反馈，已在全屏时强制切换为横屏；历史上曾增加通用 HTML5 视频浮层控制条，后续不得继续扩展该方向，播放器控制应优先回归原生 controls，仅补充缺失能力的 HUD。
- 已补充“原生播放器 UI 复用规则”，明确禁止重复实现播放 / 暂停图标、进度条、缓冲、Loading、音量、亮度、全屏、倍速、时间和字幕等播放器核心 UI。
- 已移除手势层中间点击时额外绘制的播放 / 暂停反馈图标；中间点击只调用播放器播放状态 API。
- 已移除 `common.js` 中遗留的 HTML5 自定义控制条实现，不再创建 Play / Pause 按钮、自定义 seek range、时间显示、清晰度选择和 Full 按钮；脚本只保留原生控件启用、播放器 API 调用和旧浮层 DOM 清理。
- 已修正返回/切换视频页后原生控件丢失的问题：普通导航前只暂停页面视频并退出全屏，不再把 `VideoBrowserEnhancer` 永久置为 disposed；页面开始加载时同步退出残留页面全屏状态。
- 页面标题回调已由 `ChromeClient.onReceivedTitle()` 分发给 `MainActivity`；新页面开始加载时会清空旧标题，收到有效标题后同步 Activity 标题，并继续用于收藏、历史和原生播放器标题。
- 页面加载进度回调已由 `ChromeClient.onProgressChanged()` 分发给 `MainActivity`；进度值限制在 0-100，页面完成后会结束加载状态并隐藏进度条，避免旧进度残留。
- 已在 `emulator-5554` 使用本地 HTML5 video 测试页验证全屏进入、返回键退出、播放器退出按钮退出和退出后 WebView 恢复，P4 阶段验收通过。
- 已创建 `browser/BrowserClient.kt`，`WebViewClient` 页面开始、页面完成、请求拦截和 URL 加载回调入口已从 `MainActivity` 匿名对象迁移到 `browser/` 模块。
- `MainActivity` 仅通过回调处理 UI 状态、现有广告判断和 URL 跳转决策，P5-01 模块边界验证通过。
- 已创建 `browser/BrowserRequest.kt`，请求 URL、主文档标记、方法、请求头、用户手势和重定向标记已封装为请求数据结构。
- `BrowserClient.shouldInterceptRequest()` 已统一把 `WebResourceRequest` 和旧版 URL 回调转换为 `BrowserRequest` 后再分发，P5-02 模块边界验证通过。
- 已创建 `adblock/AdBlockManager.kt`，广告拦截开关、主文档保护和内置关键字判断已从 `MainActivity` 迁移到 `adblock/` 模块，P5-03 模块边界验证通过。
- 已创建 `adblock/EmptyResponseFactory.kt`，204 No Content 空响应构造已从 `MainActivity` 迁移到 `adblock/` 模块；`MainActivity` 仅在拦截命中时调用工厂方法，P5-04 模块边界验证通过。
- 已创建 `adblock/BuiltInAdBlockRules.kt`，内置广告域名后缀和 URL 片段黑名单已从 `AdBlockManager` 拆出，P5-05 模块边界验证通过。
- 已创建 `adblock/AdBlockRequestInterceptor.kt`，`BrowserClient.shouldInterceptRequest()` 分发出的请求已通过 `MainActivity` 绑定到广告判断和空响应生成链路，P5-06 模块边界验证通过。
- 已创建 `adblock/AdBlockRequestPolicy.kt`，广告拦截开关、HTTP/HTTPS 协议检查和主文档放行策略已拆成可单元测试的纯 Kotlin 逻辑，P5-07 主文档保护验证通过。
- 已创建 `rules/Rule.kt`、`rules/RuleMatchResult.kt`、`rules/RuleEngine.kt` 和 `rules/RuleMatcher.kt`，P6 规则模型和匹配链路已放入 `rules/` 模块。
- `BuiltInAdBlockRules` 已改为内置规则源，`AdBlockManager` 通过 `RuleEngine` 获取请求级拦截结果，未向 `MainActivity` 增加新业务职责。
- `RuleEngine` 已支持 URL 包含匹配、域名边界匹配和简单白名单优先级；P6 只解析安全子集：纯 `||domain^`、URL 包含和简单 `@@` 白名单。
- 已添加 `RuleEngineTest` 覆盖命中、未命中、域名边界、白名单优先和 P6 安全子集解析。
- `gradlew.bat testDebugUnitTest` 与 `gradlew.bat assembleDebug` 已验证通过，P6 阶段验收通过。
- 已创建 `inject/ScriptLoader.kt`，脚本加载限制在本地 `assets/scripts/*.js`，并提供 `scripts/common.js` 统一入口。
- 已创建 `inject/JsInjector.kt`，页面功能配置与本地脚本注入已从 `MainActivity` 迁移到 `inject/` 模块，同一页面通过 `__VIDEOBROWSER_COMMON_SCRIPT_INSTALLED__` 避免重复加载通用脚本。
- `MainActivity.handlePageFinished()` 已通过 `injectPageFeatures()` 调用 `JsInjector.inject()`；页面清理和视频增强开关变更时复用同一注入入口。
- `common.js` 已包含 CSS 隐藏、常见广告 DOM 清理、`MutationObserver` 动态处理、保守 `window.open` / `fetch` Hook、HTML5 video 倍速 API 和常见 skip 按钮点击逻辑。
- 已添加 `ScriptLoaderTest`、`JsInjectorTest` 和 `JsInjectorInstrumentedTest`；本地单元测试、Debug 构建、Android 测试 APK 编译和 `common.js` 语法检查通过。
- 已在临时数据目录启动的 `Pixel_9a` AVD（`emulator-5560`）运行 `JsInjectorInstrumentedTest`，验证 `.ad-banner` 隐藏、skip 按钮自动点击和 HTML5 video 倍速设置在真实 WebView 中生效，P7 阶段验收通过。
- 已创建 `site/SiteAdapter.kt`、`site/SiteAdapterRegistry.kt` 和 `site/SiteProfile.kt`，站点识别和脚本选择已从 `MainActivity` 分离到 `site/` 模块。
- 已新增 `youtube.js`、`bilibili.js`、`iqiyi.js`、`tencent.js` 和 `youku.js` 站点占位脚本，脚本来源仍限制在本地 `assets/scripts/`。
- `JsInjector` 已支持按当前页面 URL 匹配站点脚本，非匹配站点只注入 `common.js`，站点脚本通过 `__VIDEOBROWSER_SITE_SCRIPT_FLAGS__` 防止同一页面重复执行。
- 已添加 `SiteAdapterRegistryTest` 和扩展 `JsInjectorTest`，覆盖域名边界、仿冒域名放行、匹配站点只加载对应脚本、非匹配站点跳过站点脚本和重复执行保护。
- `gradlew.bat testDebugUnitTest`、`gradlew.bat assembleDebug`、`gradlew.bat assembleDebugAndroidTest` 与 5 个站点脚本 `node --check` 已验证通过，P8 阶段验收通过。
- 已创建 `settings/SettingsManager.kt` 和 `storage/PreferenceStore.kt`，SharedPreferences 文件名与设置 key 统一收口到存储层和设置语义层。
- 广告请求拦截、JS 注入、DOM 去广告、视频增强、桌面模式、默认倍速、首页地址和搜索引擎已改由 `SettingsManager` 读写；收藏和历史继续复用 `PreferenceStore` 的基础字符串存储。
- `JsInjector` 已受 JS 注入开关控制，关闭后不会加载或执行 `common.js` 和站点脚本；DOM 去广告和视频增强继续作为注入脚本配置项生效。
- `gradlew.bat testDebugUnitTest`、`gradlew.bat assembleDebug` 与 `gradlew.bat assembleDebugAndroidTest` 已验证通过，P9 阶段验收通过。
- `gradlew.bat assembleDebug` 与 `gradlew.bat testDebugUnitTest` 已在 P10 阶段重新验证通过。
- 已在 `emulator-5554` 安装并启动 App，首页地址栏、搜索提供方、WebView 和底部导航可见。
- 已通过本地 P10 测试页验证地址栏 URL、关键词搜索、页面跳转、后退、前进、刷新和首页按钮。
- 已通过本地 HTML5 video 测试页验证视频播放、原生控件、全屏进入、返回键退出和前后台切换不崩溃。
- 已运行 P10 目标仪器测试，覆盖广告 URL 拦截、CSS 隐藏、DOM 清理、JS Hook、视频倍速和跳过按钮。
- P10 MVP 验收清单全部通过，未发现需要在 P10 阶段修复的阻塞缺陷。
- P11-01 已实现当前站点关闭广告请求拦截；站点 host 识别位于 `site/`，设置语义和持久化位于 `settings/` / `storage/`，请求放行判断仍位于 `adblock/`。
- `gradlew.bat testDebugUnitTest` 与 `gradlew.bat assembleDebug` 已在 P11-01 后验证通过。
- P11-02 至 P11-08 已按顺序完成：当前站点关闭 JS 注入、拦截日志、用户白名单、请求/CSS/DOM 规则文件和 5 个站点脚本保守适配。
- `gradlew.bat testDebugUnitTest`、`gradlew.bat assembleDebug`、`gradlew.bat assembleDebugAndroidTest` 与 6 个 assets 脚本 `node --check` 已在 P11 完成后验证通过。
- G0 文档门禁已按 `开发目标.md` 执行；G1 开始前已确认任务、边界和验收标准。
- 已创建 `browser/RequestContext.kt`、`browser/ResourceType.kt` 和 `browser/ResourceTypeResolver.kt`，请求 URL、页面 URL、请求 host、页面 host、主框架标记、方法、请求头和轻量资源类型已收口到请求上下文。
- `AdBlockManager` / `AdBlockRequestPolicy` / `RuleEngine` 已接入 `RequestContext`；资源类型不确定时保持 `UNKNOWN`，带资源类型限制的规则不会命中 `UNKNOWN`。
- `Rule` 解析已保存 `$script`、`$image`、`$stylesheet`、`$media`、`$font`、`$xmlhttprequest` 等安全资源类型选项；未知和暂不支持的取反资源类型选项继续跳过。
- `gradlew.bat testDebugUnitTest` 与 `gradlew.bat assembleDebug` 已在 G1 完成后验证通过。
- 已创建 `rules/RuleCompiler.kt`，把已解析的请求规则、CSS 隐藏规则、CSS 例外规则和 DOM 删除规则编译为内部能力模型。
- `CompiledRuleSet` 已预留安全 Hook 和 noop 响应能力列表，但 G2-01 不从规则文件生成或执行这些能力。
- `RuleEngine` 已改为消费 `RuleCompiler` 的编译产物，外部匹配 API、白名单优先级、资源类型限制和页面选择器输出保持兼容。
- 已添加 `RuleCompilerTest` 覆盖能力分类、原始规则对象保留和诊断来源保留。
- `gradlew.bat testDebugUnitTest` 与 `gradlew.bat assembleDebug` 已在 G2-01 完成后验证通过。
- 已新增 `RuleCapabilityKind`，显式区分请求能力、元素隐藏能力、DOM 删除能力、安全 Hook 能力和 noop 响应能力。
- CSS 隐藏和 CSS 例外统一归入元素隐藏能力，并通过 `ElementHideEffect.HIDE / UNHIDE` 区分具体效果；DOM 删除保持独立能力。
- `CompiledRuleSet.allCapabilities()` 和 `elementHideCapabilities()` 已提供稳定能力列表入口，供后续索引、决策和诊断层复用。
- `RuleCapability.SafeHook` 和 `RuleCapability.NoopResponse` 已具备独立 kind，但仍只作为后续阶段预留，不从当前规则文件生成或执行。
- `gradlew.bat testDebugUnitTest` 与 `gradlew.bat assembleDebug` 已在 G2-02 完成后验证通过。
- 已新增 `RequestRuleIndex`，在编译产物内为 `RuleType.DOMAIN_CONTAINS` 请求规则建立 host / suffix 候选索引。
- `RuleEngine` 请求匹配已改为从 `CompiledRuleSet.requestCandidatesFor()` 获取候选规则；URL contains / URL pattern 规则继续走 fallback，保持原有规则顺序和白名单优先语义。
- 已添加 `RuleCompilerTest` 和 `RuleEngineTest` 覆盖域名后缀候选、未索引 fallback、白名单/阻断候选分离、host 缺失时从 URL 解析以及索引后命中顺序兼容。
- `gradlew.bat testDebugUnitTest` 与 `gradlew.bat assembleDebug` 已在 G2-03 完成后验证通过。

---

## 5. 阶段总览

| 阶段 | 名称 | 状态 | 完成度 | 目标 |
| --- | --- | --- | --- | --- |
| P0 | 工程基线与现状确认 | `[x]` | 100% | 确认项目可构建、可启动、具备基础 WebView |
| P1 | 主页面 UI 与基础导航 | `[x]` | 100% | 实现地址栏、搜索、前进、后退、刷新、首页 |
| P2 | Browser Core 拆分 | `[x]` | 100% | 引入 `BrowserManager`、`BrowserClient`、`ChromeClient` |
| P3 | URL 与搜索解析 | `[x]` | 100% | 引入 `UrlUtils`，规范地址栏输入处理 |
| P4 | 视频全屏与页面状态 | `[x]` | 100% | 支持视频全屏、标题、进度回调 |
| P5 | 基础广告请求拦截 | `[x]` | 100% | 实现基础 URL 黑名单与空响应 |
| P6 | 规则系统 MVP | `[x]` | 100% | 封装规则数据结构、匹配结果和规则加载 |
| P7 | JS 注入与 assets 脚本 | `[x]` | 100% | 注入 CSS、DOM、JS Hook、视频增强脚本 |
| P8 | 站点适配框架 | `[x]` | 100% | 按域名加载站点脚本 |
| P9 | 设置与本地存储 | `[x]` | 100% | 使用 SharedPreferences 保存开关和默认值 |
| P10 | MVP 验收测试 | `[x]` | 100% | 按验收清单完成整体测试 |
| P11 | v1.0 增强 | `[x]` | 100% | 白名单、日志、规则文件、站点完善 |
| P12 | AdGuard 规则兼容层 | `[~]` | 10% | 分阶段接入 AdGuard 规则语法和订阅 |
| G0 | 文档门禁 | `[x]` | 100% | 建立新阶段先补 Markdown、完成后回写验证的约束 |
| G1 | 请求上下文轻量增强 | `[x]` | 100% | 补齐请求上下文、资源类型推断和类型限制匹配 |
| G2 | 规则编译与索引 | `[~]` | 42% | 编译内部能力模型并建立匹配索引 |

---

## 6. P0 工程基线与现状确认

目标：确认项目具备继续开发的 Android 基线。

进入条件：

- 已有 Android 工程。
- 已有 `app` 模块。

任务清单：

- `[x]` P0-01 确认 Kotlin Android 工程结构存在。
- `[x]` P0-02 确认 `MainActivity.kt` 存在。
- `[x]` P0-03 确认 `activity_main.xml` 存在。
- `[x]` P0-04 确认 `AndroidManifest.xml` 已声明 `INTERNET` 权限。
- `[x]` P0-05 确认 `WebView` 已初始化并加载默认首页。
- `[x]` P0-06 确认 WebView 已启用 JavaScript、DOM Storage、HTML5 视频免手势播放。
- `[x]` P0-07 执行一次 `gradlew.bat assembleDebug`，确认当前工程可构建。
- `[x]` P0-08 在设备或模拟器启动一次 App，确认首页可打开。

阶段验收标准：

- Debug 包可构建。
- App 可启动。
- 首页能加载。
- WebView 不因基础配置崩溃。

当前完成度：`100%`

阻塞或风险：

- P0 无阻塞。

---

## 7. P1 主页面 UI 与基础导航

目标：完成 MVP 浏览器最基本的可操作界面。

进入条件：

- P0 的代码基线已确认。
- 当前可以安全修改 `activity_main.xml` 和 `MainActivity.kt`。

任务清单：

- `[x]` P1-01 重构 `activity_main.xml`，增加顶部地址栏、WebView 区域、底部导航栏。
- `[x]` P1-02 在 `MainActivity` 绑定地址栏和底部按钮。
- `[x]` P1-03 实现地址栏回车或搜索按钮触发加载。
- `[x]` P1-04 实现返回按钮：能返回时 `goBack()`，不能返回时保持当前页。
- `[x]` P1-05 实现前进按钮：能前进时 `goForward()`。
- `[x]` P1-06 实现刷新按钮：调用 `reload()`。
- `[x]` P1-07 实现首页按钮：加载默认首页。
- `[x]` P1-08 页面加载完成后，同步地址栏显示当前 URL。
- `[x]` P1-09 根据 `canGoBack()` 和 `canGoForward()` 更新按钮可用状态。
- `[x]` P1-10 验证竖屏下地址栏、WebView、底部栏布局正常。

阶段验收标准：

- 主界面包含地址栏、WebView、底部导航栏。
- 输入完整 URL 可以打开。
- 输入域名可以打开。
- 输入关键词可以搜索。
- 返回、前进、刷新、首页按钮可用。
- 软键盘搜索动作可触发加载。
- 页面加载后地址栏能显示当前页面 URL。

当前完成度：`100%`

推荐实现顺序：

1. 先改 XML 布局，不引入复杂样式。
2. 再在 `MainActivity` 直接绑定控件。
3. URL 解析先写最小可用逻辑，后续 P3 再迁移到 `UrlUtils`。

---

## 8. P2 Browser Core 拆分

目标：把 WebView 初始化和基础操作从 `MainActivity` 迁移到浏览器核心模块。

进入条件：

- P1 已完成并通过基础导航验收。

任务清单：

- `[x]` P2-01 创建 `browser/BrowserManager.kt`，作为浏览器核心入口。
- `[x]` P2-02 将 WebView Settings 初始化迁移到 `BrowserManager.setup()`。
- `[x]` P2-03 将 `load()`、`goBack()`、`goForward()`、`reload()` 封装到 `BrowserManager`。
- `[x]` P2-04 将 `onPause()`、`onResume()`、`destroy()` 生命周期封装到 `BrowserManager`。
- `[x]` P2-05 `MainActivity` 只保留 UI 绑定和生命周期分发。
- `[x]` P2-06 验证 P1 的所有导航功能不回退。

阶段验收标准：

- `MainActivity` 不再直接承担 WebView 配置细节。
- `MainActivity` 不再继续承载 URL 解析、请求拦截、脚本注入、设置读写等跨功能逻辑。
- 新增或迁移的代码按 `browser/`、`utils/`、`adblock/`、`rules/`、`inject/`、`site/`、`settings/`、`storage/` 的功能边界放置。
- 页面加载、返回、前进、刷新、首页功能保持可用。
- 生命周期销毁逻辑不崩溃。

当前完成度：`100%`

---

## 9. P3 URL 与搜索解析

目标：规范地址栏输入处理。

进入条件：

- P1 已完成。
- P2 建议已完成。

任务清单：

- `[x]` P3-01 创建 `utils/UrlUtils.kt`。
- `[x]` P3-02 支持 `http://` 和 `https://` 直接加载。
- `[x]` P3-03 支持裸域名自动补全为 `https://`。
- `[x]` P3-04 支持普通文本转搜索 URL。
- `[x]` P3-05 对空输入、空白输入做保护。
- `[x]` P3-06 添加 `UrlUtils` 单元测试。

阶段验收标准：

- `https://example.com` 保持原样。
- `example.com` 转为 `https://example.com`。
- `hello world` 转为搜索引擎地址。
- 空输入不触发无效加载。

当前完成度：`100%`

---

## 10. P4 视频全屏与页面状态

目标：支持 WebChromeClient 基础能力。

进入条件：

- P2 已完成。

任务清单：

- `[x]` P4-01 创建 `browser/ChromeClient.kt`。
- `[x]` P4-02 实现 `onShowCustomView()`。
- `[x]` P4-03 实现 `onHideCustomView()`。
- `[x]` P4-04 增加全屏容器布局。
- `[x]` P4-05 支持页面标题回调。
- `[x]` P4-06 支持页面加载进度回调。
- `[x]` P4-07 验证 HTML5 视频全屏进入和退出。

阶段验收标准：

- 普通 HTML5 video 可进入全屏。
- 返回键或播放器退出按钮能退出全屏。
- 全屏退出后 WebView 正常显示。

当前完成度：`100%`

---

## 11. P5 基础广告请求拦截

目标：在请求级别拦截明显广告资源。

进入条件：

- P2 已完成。

任务清单：

- `[x]` P5-01 创建 `browser/BrowserClient.kt`。
- `[x]` P5-02 创建 `browser/BrowserRequest.kt`。
- `[x]` P5-03 创建 `adblock/AdBlockManager.kt`。
- `[x]` P5-04 创建 `adblock/EmptyResponseFactory.kt`。
- `[x]` P5-05 实现内置 URL 黑名单。
- `[x]` P5-06 在 `shouldInterceptRequest()` 中接入拦截判断。
- `[x]` P5-07 避免拦截主文档请求。
- `[x]` P5-08 验证命中广告 URL 返回空响应。

阶段验收标准：

- 命中 `doubleclick.net`、`/pagead/` 等规则的资源请求被拦截。
- 普通网页主文档不被误杀。
- 普通图片和普通 JS 不被明显误杀。

当前完成度：`100%`

---

## 12. P6 规则系统 MVP

目标：把硬编码黑名单迁移到规则系统，为后续规则文件做准备。

进入条件：

- P5 已完成。

任务清单：

- `[x]` P6-01 创建 `rules/Rule.kt`。
- `[x]` P6-02 创建 `rules/RuleMatchResult.kt`。
- `[x]` P6-03 创建 `rules/RuleEngine.kt`。
- `[x]` P6-04 创建 `rules/RuleMatcher.kt`。
- `[x]` P6-05 支持 URL 包含匹配。
- `[x]` P6-06 支持域名包含匹配。
- `[x]` P6-07 支持简单白名单规则。
- `[x]` P6-08 为规则匹配添加单元测试。

阶段验收标准：

- 广告拦截由 `RuleEngine` 给出结果。
- 白名单优先于屏蔽规则。
- 规则测试覆盖命中、未命中、白名单三类场景。

AdGuard 接入边界：

- P6 完成后只允许接入请求级安全子集：纯 `||domain^` 域名规则、URL 包含规则和简单 `@@` 白名单。
- 暂不接入 `$third-party`、`$domain=`、资源类型选项、`$removeparam`、`$redirect`、`$replace`、CSS 选择器和 scriptlet。
- P6 不下载远程订阅，不直接导入完整 AdGuard 文件；如需验证，只使用少量手工挑选的本地测试规则。

当前完成度：`100%`

---

## 13. P7 JS 注入与 assets 脚本

目标：实现 CSS 隐藏、DOM 删除、JS Hook 和视频增强基础逻辑。

进入条件：

- P2 已完成。
- P6 建议已完成。

任务清单：

- `[x]` P7-01 创建 `inject/ScriptLoader.kt`。
- `[x]` P7-02 创建 `inject/JsInjector.kt`。
- `[x]` P7-03 创建 `app/src/main/assets/scripts/common.js`。
- `[x]` P7-04 `common.js` 实现 CSS 隐藏规则注入。
- `[x]` P7-05 `common.js` 实现常见广告 DOM 删除。
- `[x]` P7-06 `common.js` 实现 MutationObserver 动态删除。
- `[x]` P7-07 `common.js` 实现保守的 `window.open`、`fetch` Hook。
- `[x]` P7-08 `common.js` 实现 video 倍速设置。
- `[x]` P7-09 `common.js` 实现常见跳过广告按钮自动点击。
- `[x]` P7-10 页面加载完成后调用 `JsInjector.inject()`。

阶段验收标准：

- 脚本只从本地 assets 加载。
- 同一页面避免重复注入。
- `.ad-banner` 等测试节点能隐藏或删除。
- HTML5 video 倍速设置可生效。
- 常见 skip 按钮能被自动点击。

AdGuard 接入边界：

- P7 完成后可开始接入通用 `##` CSS 隐藏规则，并通过本地 JS 注入实现 DOM 清理。
- `#%#` scriptlet 不做通用解释执行，只允许映射到项目已实现的保守 Hook，例如 `window.open`、`fetch` 屏蔽。
- 未知 CSS 伪类、复杂选择器和高风险 JS 规则必须丢弃或记录，不允许直接注入。

验收结果：

- 已在临时数据目录启动的 `Pixel_9a` AVD（`emulator-5560`）执行 `JsInjectorInstrumentedTest`，确认 `.ad-banner` 隐藏、skip 按钮自动点击和 video 倍速在真实 WebView 中生效。
- 真机 `2312DRA50C` 因系统策略返回 `INSTALL_FAILED_USER_RESTRICTED`，测试 APK 安装被设备取消；本次不作为 P7 阶段阻塞，运行时验收以干净 AVD 结果为准。

当前完成度：`100%`

---

## 14. P8 站点适配框架

目标：支持按域名加载站点专属脚本。

进入条件：

- P7 已完成。

任务清单：

- `[x]` P8-01 创建 `site/SiteAdapter.kt`。
- `[x]` P8-02 创建 `site/SiteAdapterRegistry.kt`。
- `[x]` P8-03 创建 `site/SiteProfile.kt`。
- `[x]` P8-04 创建 `youtube.js`、`bilibili.js`、`iqiyi.js`、`tencent.js`、`youku.js` 占位脚本。
- `[x]` P8-05 按域名匹配脚本文件。
- `[x]` P8-06 页面加载完成后注入匹配站点脚本。

阶段验收标准：

- 访问匹配域名时只注入对应站点脚本。
- 非匹配站点只注入通用脚本。
- 站点脚本重复注入有保护。

AdGuard 接入边界：

- P8 完成后可接入 `example.com##.ad`、`example.com#@#.ad` 这类站点限定 CSS 规则。
- 站点限定规则必须按当前页面 host 匹配后再注入，不能作为全局 CSS 使用。
- 视频站点规则优先进入站点脚本适配，不直接塞入通用脚本，避免误伤普通网页。

当前完成度：`100%`

---

## 15. P9 设置与本地存储

目标：保存 MVP 需要的开关和默认值。

进入条件：

- P1 已完成。
- P7 建议已完成。

任务清单：

- `[x]` P9-01 创建 `settings/SettingsManager.kt`。
- `[x]` P9-02 创建 `storage/PreferenceStore.kt`。
- `[x]` P9-03 保存广告拦截开关。
- `[x]` P9-04 保存 JS 注入开关。
- `[x]` P9-05 保存 DOM 去广告开关。
- `[x]` P9-06 保存视频增强开关。
- `[x]` P9-07 保存默认倍速。
- `[x]` P9-08 保存首页地址。
- `[x]` P9-09 保存搜索引擎。
- `[x]` P9-10 先提供代码层默认值，设置页 UI 可后续实现。

阶段验收标准：

- 关闭广告拦截后，请求拦截不再生效。
- 关闭 JS 注入后，不再执行 `common.js`。
- 首页和搜索引擎可以从设置读取。

AdGuard 接入边界：

- P9 完成后，规则接入必须受广告拦截、JS 注入、DOM 去广告开关控制。
- 扩大规则规模前必须保证用户能关闭请求拦截和页面注入，避免误杀后无法恢复浏览。

完成说明：

- `SettingsManager` 提供代码层默认值和设置语义 API，`PreferenceStore` 负责 SharedPreferences 基础读写。
- `MainActivity` 已通过 `SettingsManager` 控制广告请求拦截、JS 注入、DOM 去广告、视频增强、默认倍速、首页地址和搜索引擎。
- 关闭 JS 注入后，`JsInjector` 直接跳过 `common.js` 和站点脚本加载执行。

当前完成度：`100%`

---

## 16. P10 MVP 验收测试

目标：确认 MVP 可交付。

进入条件：

- P1 到 P9 已完成。

任务清单：

- `[x]` P10-01 执行 `gradlew.bat assembleDebug`。
- `[x]` P10-02 执行现有单元测试。
- `[x]` P10-03 App 启动测试。
- `[x]` P10-04 首页加载测试。
- `[x]` P10-05 地址栏 URL 测试。
- `[x]` P10-06 地址栏关键词搜索测试。
- `[x]` P10-07 返回、前进、刷新、首页测试。
- `[x]` P10-08 HTML5 video 播放测试。
- `[x]` P10-09 视频全屏测试。
- `[x]` P10-10 广告 URL 拦截测试。
- `[x]` P10-11 CSS 隐藏和 DOM 删除测试。
- `[x]` P10-12 JS Hook 基础测试。
- `[x]` P10-13 视频倍速和跳过按钮测试。
- `[x]` P10-14 前后台切换不崩溃测试。

阶段验收标准：

- MVP 完成标准全部通过。
- 本文件的验证记录已更新。
- 未完成项已明确归入 v1.x 或后续阶段。

验证备注：

- P10 只执行验收测试和必要临时测试页准备，未改动 App 源码。
- 本次使用本地 P10 测试页，通过模拟器访问 `10.0.2.2:8765` 验证 URL、导航、视频和页面清理能力。
- MVP 验收项全部通过；P11/P12 范围仍按后续阶段处理。

当前完成度：`100%`

---

## 17. P11 v1.0 增强

目标：在 MVP 之后完善规则、日志、白名单和站点适配。

进入条件：

- P10 已完成。

任务清单：

- `[x]` P11-01 当前站点关闭广告拦截。
- `[x]` P11-02 当前站点关闭 JS 注入。
- `[x]` P11-03 拦截日志。
- `[x]` P11-04 用户白名单管理。
- `[x]` P11-05 规则文件加载，先支持本地 assets 和缓存文件。
- `[x]` P11-06 CSS 规则文件，按支持语法筛选后注入。
- `[x]` P11-07 DOM 规则文件，按支持语法转换为本地清理逻辑。
- `[x]` P11-08 5 个站点脚本适配完善。

阶段验收标准：

- 规则文件加载失败时继续使用内置规则，不影响页面打开。
- 拦截日志能说明命中的规则来源、规则文本或规则 ID。
- 用户白名单优先级高于内置规则和规则文件。
- CSS / DOM 规则文件只启用当前已支持语法，未知规则被跳过并可在日志中定位。

AdGuard 接入边界：

- P11 可以正式接入规则文件，但仍是“支持语法子集”，不是完整 AdGuard 兼容。
- 扩大导入规模前必须具备拦截日志、用户白名单和开关控制。
- 远程订阅下载、增量更新和复杂语法兼容放到 P12。

验证备注：

- P11-01 新增 `site/SiteHost.kt` 统一当前站点 host 识别；站点级关闭状态由 `SettingsManager` 通过现有 `PreferenceStore` 保存，未引入 Room、DataStore、OkHttp、远程订阅或规则文件加载。
- `AdBlockManager` 和 `AdBlockRequestPolicy` 已接入当前站点关闭状态；当前站点关闭后子资源广告请求放行，主文档保护、非 HTTP 放行和全局广告拦截开关行为保持不变。
- 功能中心新增“当前站点关闭广告拦截”开关；无可识别当前 host 时该项禁用，避免写入无效站点配置。
- 新增本地单元测试覆盖 host 规范化、站点级关闭状态持久化、恢复默认设置清理站点配置，以及当前站点关闭时广告请求放行。
- P11-02 复用 `SiteHost` 和站点级设置模式，新增当前站点关闭 JS 注入；关闭后 `common.js` 和站点脚本均不执行。
- P11-03 新增 `AdBlockDecision`、`AdBlockLogEntry` 和 `AdBlockLogger`；拦截日志记录规则来源、规则 ID、规则文本和用户白名单放行原因。
- P11-04 新增用户请求域名白名单，优先级高于内置规则和规则文件；功能中心可查看、添加当前站点、从日志命中项添加或移除白名单。
- P11-05 新增 `RuleFileLoader`，支持从 `assets/rules` 和 `filesDir/rules` 缓存目录加载请求规则；加载失败或未知语法会跳过并写入 logcat。
- P11-06/P11-07 新增 `ElementRule`，CSS 隐藏规则注入到 `common.js` 配置，DOM 删除规则转换为本地清理逻辑；只启用当前安全子集。
- P11-08 将 `youtube.js`、`bilibili.js`、`iqiyi.js`、`tencent.js`、`youku.js` 从占位脚本改为保守站点适配，覆盖广告容器隐藏、跳过/关闭按钮和视频 controls。

当前完成度：`100%`

---

## 18. P12 AdGuard 规则兼容层

目标：在 P11 的规则文件能力之上，逐步提高 AdGuard 规则兼容度，支持订阅导入、规则缓存和复杂语法的安全子集。

进入条件：

- P11-03 拦截日志已完成。
- P11-04 用户白名单管理已完成。
- P11-05 到 P11-07 规则文件能力已完成。
- P9 的广告拦截、JS 注入和 DOM 去广告开关已完成。

任务清单：

- `[ ]` P12-01 创建 `adguard/AdGuardRuleParser.kt`，把 AdGuard 文本规则解析为项目内部规则模型。
- `[ ]` P12-02 支持请求规则安全子集导入：`||domain^`、URL 包含、锚定 URL 和可控正则。
- `[ ]` P12-03 支持 `@@` 白名单规则和白名单优先级。
- `[x]` P12-04 补充请求上下文，支持 `$third-party`、`$domain=` 和常见资源类型选项。
- `[ ]` P12-05 支持 `##`、`#@#` CSS 隐藏和例外规则按当前页面 host 生效。
- `[ ]` P12-06 支持保守 scriptlet 映射，只启用项目已实现 Hook，未知 scriptlet 默认跳过。
- `[ ]` P12-07 支持 `$removeparam` 导航 URL 清理，默认不处理子资源请求参数。
- `[ ]` P12-08 建立规则索引，避免每个请求线性扫描完整订阅。
- `[ ]` P12-09 支持订阅导入、版本缓存、更新失败回退和规则来源标记。
- `[ ]` P12-10 添加兼容性测试、误杀回归测试和大规则文件性能测试。

接入顺序：

1. 先接入请求级安全子集，验证普通网页、图片、JS 和视频资源不明显误杀。
2. 再接入白名单和规则选项，确保例外规则优先级正确。
3. 再接入 CSS / DOM 规则，必须按页面 host 生效。
4. 最后接入 scriptlet、`$removeparam` 和订阅更新，所有未知或高风险语法默认跳过。

阶段验收标准：

- AdGuard 中文过滤器可以导入并只启用当前支持的规则子集。
- 未支持规则不会崩溃、不会直接执行、不会影响已支持规则。
- 规则命中、白名单放行、CSS 注入、订阅更新失败回退都有测试覆盖。
- 大规则文件导入和请求匹配不会造成明显卡顿。

当前完成度：`10%`

G1 对应实现记录：

- `RequestContext` 已承接请求 URL、页面 URL、请求 host、页面 host、主框架标记、方法、请求头和轻量资源类型。
- `ResourceTypeResolver` 只通过 `isForMainFrame`、请求头和 URL 后缀做保守推断；无法确认时返回 `UNKNOWN`。
- 请求规则资源类型选项已进入 `Rule.resourceTypes`，`UNKNOWN` 不命中带类型限制的规则，避免资源类型不确定时误杀。
- 资源类型上下文位于 `browser/`，规则选项解析和匹配仍位于 `rules/`，请求拦截策略仍位于 `adblock/`，未向 `MainActivity` 增加新业务职责。

G2 对应实现记录：

- G2-01 新增 `RuleCompiler` 和 `CompiledRuleSet`，把已支持请求规则、CSS 隐藏、CSS 例外和 DOM 删除规则转换为显式能力列表。
- `RuleCapability.SafeHook` 和 `RuleCapability.NoopResponse` 仅作为后续能力模型预留，当前阶段不解析、不生成、不执行对应能力。
- `RuleEngine` 已改为基于编译产物做请求匹配、CSS 选择器输出、DOM 选择器输出和页面 Hook URL 关键字输出，模块边界仍位于 `rules/`。
- 已新增 `RuleCompilerTest`，并通过现有 `RuleEngineTest` 验证编译层接入后匹配结果保持一致。
- G2-02 新增 `RuleCapabilityKind`，把能力稳定分为 `REQUEST`、`ELEMENT_HIDE`、`DOM_REMOVE`、`SAFE_HOOK` 和 `NOOP_RESPONSE`。
- `ElementHideEffect` 已区分 CSS 隐藏和 CSS 例外，避免后续页面层把例外规则误当作 DOM 删除或请求规则处理。
- `CompiledRuleSet` 已提供全部能力列表和元素隐藏能力列表；后续索引可以基于 kind 分组，不再依赖外部判断 `RuleType` 或 `ElementRuleType`。
- G2-03 新增 `RequestRuleIndex`，只把域名请求规则索引到 host / suffix 候选表，URL contains 和 URL pattern 规则继续保留在 fallback。
- `RuleEngine` 已通过 `CompiledRuleSet.requestCandidatesFor()` 获取请求候选规则，再交给 `RuleMatcher` 做资源类型、站点限定、第三方和最终模式匹配，索引层不绕过 G1 上下文语义。
- 已新增测试验证索引不会引入无关域名规则，且同一动作内继续按原始规则顺序匹配；缺失 request host 时仍可从 URL 解析 host 命中域名规则。

当前完成度：`42%`

---

## 19. 验证命令记录

| 日期 | 命令或验证 | 结果 | 备注 |
| --- | --- | --- | --- |
| 2026-05-29 | `gradlew.bat testDebugUnitTest` | 通过 | G2-03 新增域名 host / suffix 索引测试，并回归现有 RuleEngine、广告策略、注入和设置单元测试 |
| 2026-05-29 | `gradlew.bat assembleDebug` | 通过 | G2-03 `RequestRuleIndex` 接入编译产物和 `RuleEngine` 后 Debug 包构建成功；未索引请求规则继续走 fallback |
| 2026-05-29 | `gradlew.bat testDebugUnitTest` | 通过 | G2-02 新增能力 kind/effect 测试，并回归现有 RuleEngine、广告策略、注入和设置单元测试 |
| 2026-05-29 | `gradlew.bat assembleDebug` | 通过 | G2-02 显式能力分类模型接入后 Debug 包构建成功；安全 Hook 和 noop 响应仍为预留能力，不在当前阶段执行 |
| 2026-05-29 | `gradlew.bat testDebugUnitTest` | 通过 | G2-01 新增 `RuleCompilerTest`，并回归现有规则、广告策略、注入和设置单元测试；编译层接入后规则匹配结果保持兼容 |
| 2026-05-29 | `gradlew.bat assembleDebug` | 通过 | G2-01 `RuleCompiler`、`CompiledRuleSet` 和 `RuleEngine` 编译产物接入后 Debug 包构建成功；新逻辑仍限制在 `rules/` 模块 |
| 2026-05-29 | `gradlew.bat testDebugUnitTest` | 通过 | G1 新增 `RequestContext`、`ResourceType`、`ResourceTypeResolver`，扩展请求规则资源类型匹配；覆盖主文档、脚本、图片、媒体、未知类型和 UNKNOWN 保守不命中 |
| 2026-05-29 | `gradlew.bat assembleDebug` | 通过 | G1 请求上下文接入 AdBlockManager、AdBlockRequestPolicy 和 RuleEngine 后 Debug 包构建成功；BrowserClient 继续只分发 WebView 请求 |
| 2026-05-29 | `node --check app/src/main/assets/scripts/common.js`、`youtube.js`、`bilibili.js`、`iqiyi.js`、`tencent.js`、`youku.js` | 通过 | P11-08 完成后 6 个 assets 脚本语法检查通过 |
| 2026-05-29 | `gradlew.bat testDebugUnitTest` | 通过 | P11-02 至 P11-07 新增和扩展设置、拦截策略、日志、规则文件、元素规则和注入配置测试 |
| 2026-05-29 | `gradlew.bat assembleDebug` | 通过 | P11-02 至 P11-08 代码、资源、assets 规则文件和站点脚本编译打包成功 |
| 2026-05-29 | `gradlew.bat assembleDebugAndroidTest` | 通过 | P11 注入配置接口变化后 Android 测试 APK 编译成功 |
| 2026-05-29 | `gradlew.bat testDebugUnitTest` | 通过 | P11-01 新增 `SiteHostTest`，扩展 `SettingsManagerTest` 与 `AdBlockRequestPolicyTest`；覆盖当前站点广告拦截关闭状态的保存、恢复和请求放行 |
| 2026-05-29 | `gradlew.bat assembleDebug` | 通过 | P11-01 当前站点广告拦截关闭开关、字符串资源和请求拦截链路编译打包成功 |
| 2026-05-29 | `gradlew.bat assembleDebug` | 通过 | P10-01 Debug 包构建成功 |
| 2026-05-29 | `gradlew.bat testDebugUnitTest` | 通过 | P10-02 现有本地单元测试通过 |
| 2026-05-29 | `gradlew.bat installDebug`、`adb -s emulator-5554 shell am start -n com.example.videobrowser/.MainActivity` | 通过 | P10-03/P10-04 App 安装启动成功；UI dump 显示地址栏、搜索提供方、WebView 和底部导航 |
| 2026-05-29 | `emulator-5554` 加载本地 P10 测试页 | 通过 | P10-05 地址栏 URL `http://10.0.2.2:8765/index.html` 可打开，WebView 显示 `P10 Page One` |
| 2026-05-29 | `emulator-5554` 输入关键词 `hello world` | 通过 | P10-06 地址栏解析为 `https://m.baidu.com/s?ie=utf-8&word=hello+world`，页面标题显示 `hello world - 百度` |
| 2026-05-29 | `emulator-5554` 点击本地页面链接、后退、前进、刷新、首页 | 通过 | P10-07 Page One/Page Two 跳转正常；后退、前进、刷新和首页按钮状态与页面一致 |
| 2026-05-29 | `gradlew.bat connectedDebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=com.example.videobrowser.adblock.AdBlockRequestInterceptorInstrumentedTest,com.example.videobrowser.inject.JsInjectorInstrumentedTest` | 通过 | P10-10/P10-11/P10-12/P10-13 在 `emulator-5554` 运行 3 个仪器测试通过；覆盖广告 URL 拦截、CSS/DOM 清理、JS Hook、视频倍速和 skip 自动点击 |
| 2026-05-29 | `emulator-5554` 加载本地 `video.html` 测试页 | 通过 | P10-08/P10-09/P10-11/P10-13 通过；`sample.mp4` 请求成功，视频原生 controls 可见，播放后时间轴到 `0:01`，全屏进入横屏，返回键退出回竖屏，页面结果显示脚本已注入、广告元素隐藏、skip 已点击 |
| 2026-05-29 | `emulator-5554` Home/恢复前后台切换 | 通过 | P10-14 通过；进程在前后台切换前后保持存在，恢复后仍显示视频页，日志未出现 FATAL 或 ANR |
| 2026-05-28 | `gradlew.bat assembleDebugAndroidTest` | 通过 | P9 变更后 Android 测试 APK 编译成功；首次运行受沙箱网络限制，授权 Gradle 分发访问后通过 |
| 2026-05-28 | `gradlew.bat testDebugUnitTest` | 通过 | P9 新增 `SettingsManagerTest` 并扩展 `JsInjectorTest`；覆盖设置默认值、持久化、恢复默认、默认倍速、首页/搜索引擎读取以及关闭 JS 注入时跳过 `common.js` |
| 2026-05-28 | `gradlew.bat assembleDebug` | 通过 | P9 新增设置与本地存储分层，并接入 MainActivity、JsInjector 和 PlayerActivity 后 Debug 包构建成功 |
| 2026-05-28 | `gradlew.bat testDebugUnitTest` | 通过 | P8 新增 `SiteAdapterRegistryTest` 并扩展 `JsInjectorTest`；覆盖站点域名边界、匹配脚本选择、非匹配站点跳过站点脚本和重复执行保护 |
| 2026-05-28 | `gradlew.bat assembleDebug` | 通过 | P8 接入站点脚本匹配与本地 assets 占位脚本后 Debug 包构建成功 |
| 2026-05-28 | `gradlew.bat assembleDebugAndroidTest` | 通过 | P8 变更后 Android 测试 APK 编译成功；首次运行受沙箱网络限制，授权下载缺失 Gradle 分发后通过 |
| 2026-05-28 | `node --check app/src/main/assets/scripts/youtube.js` 等 5 个站点脚本 | 通过 | `youtube.js`、`bilibili.js`、`iqiyi.js`、`tencent.js`、`youku.js` 占位脚本语法检查通过 |
| 2026-05-28 | `gradlew.bat connectedDebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=com.example.videobrowser.inject.JsInjectorInstrumentedTest` | 通过 | P7 目标仪器测试在临时数据目录启动的 `Pixel_9a` AVD（`emulator-5560`）通过；覆盖 WebView 中 `.ad-banner` 隐藏、skip 点击和 video 倍速 |
| 2026-05-28 | `gradlew.bat connectedDebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=com.example.videobrowser.inject.JsInjectorInstrumentedTest` | 受设备限制未通过 | 真机 `2312DRA50C` 返回 `INSTALL_FAILED_USER_RESTRICTED`，测试 APK 安装被系统策略取消；改用干净 AVD 完成验收 |
| 2026-05-28 | `gradlew.bat testDebugUnitTest` | 通过 | P7 新增 `ScriptLoaderTest` 和 `JsInjectorTest`，覆盖本地 assets 脚本加载、路径限制、注入脚本配置和重复调用时复用已加载脚本 |
| 2026-05-28 | `gradlew.bat assembleDebug` | 通过 | P7 将 `common.js` 读取和页面注入迁移到 `inject/` 模块后 Debug 包构建成功 |
| 2026-05-28 | `gradlew.bat assembleDebugAndroidTest` | 通过 | P7 新增 `JsInjectorInstrumentedTest` 后 Android 测试 APK 编译成功；该测试覆盖 WebView 中 `.ad-banner` 隐藏、skip 点击和 video 倍速 |
| 2026-05-28 | `node --check app/src/main/assets/scripts/common.js` | 通过 | P7 通用脚本语法检查通过 |
| 2026-05-28 | 文档更新检查 | 通过 | 补充 AdGuard 规则按 P6 到 P12 逐步接入路线；本次只更新文档，未改代码 |
| 2026-05-28 | `gradlew.bat testDebugUnitTest` | 通过 | P6 新增 `rules/` 规则模型、匹配器、引擎和白名单优先级测试；现有 P5 广告策略测试通过 |
| 2026-05-28 | `gradlew.bat assembleDebug` | 通过 | P6 将内置黑名单迁移为规则源，并由 `AdBlockManager` 接入 `RuleEngine` 后 Debug 包构建成功 |
| 2026-05-25 | 读取工程文件 | 通过 | 已确认当前工程结构、Manifest、MainActivity、布局现状 |
| 2026-05-26 | `gradlew.bat assembleDebug` | 通过 | Debug 包构建成功 |
| 2026-05-26 | `adb -s emulator-5554 install -r app/build/outputs/apk/debug/app-debug.apk` | 通过 | 安装到模拟器成功 |
| 2026-05-26 | `adb -s emulator-5554 shell am start -n com.example.videobrowser/.MainActivity` | 通过 | 主 Activity 进入 RESUMED，界面 dump 显示首页基础 UI 与 WebView |
| 2026-05-26 | `gradlew.bat assembleDebug` | 通过 | P1 导航状态修正后 Debug 包构建成功，仅保留 P4 全屏相关 deprecated 警告 |
| 2026-05-26 | `adb -s emulator-5554 install -r app/build/outputs/apk/debug/app-debug.apk` | 通过 | P1 版本安装到模拟器成功 |
| 2026-05-26 | `adb -s emulator-5554 shell am start -n com.example.videobrowser/.MainActivity` | 通过 | App 启动成功 |
| 2026-05-26 | `adb -s emulator-5554 shell uiautomator dump /sdcard/videobrowser-window.xml` | 通过 | 竖屏 `rotation="0"`，UI dump 显示地址栏、WebView 容器、WebView 和底部导航栏 |
| 2026-05-26 | 模拟器输入 `example.com` 并按回车 | 通过 | 地址栏同步为 `https://example.com/`，WebView 显示 Example Domain，后退按钮变为可用 |
| 2026-05-26 | `gradlew.bat assembleDebug` | 通过 | P2-01 创建 `BrowserManager` 入口后 Debug 包构建成功，仅保留既有 P4 全屏相关 deprecated 警告；新增代码位于 `browser/` 模块边界内 |
| 2026-05-26 | `gradlew.bat assembleDebug` | 通过 | P2-02 将 `WebView.settings` 初始化迁移到 `BrowserManager.setup()` 后 Debug 包构建成功，仅保留既有 P4 全屏相关 deprecated 警告 |
| 2026-05-26 | `gradlew.bat assembleDebug` | 通过 | P2-03 将加载、返回、前进、刷新操作封装到 `BrowserManager` 后 Debug 包构建成功，仅保留既有 P4 全屏相关 deprecated 警告 |
| 2026-05-26 | `gradlew.bat assembleDebug` | 通过 | P2-04 将 `onPause()`、`onResume()`、`destroy()` 生命周期入口封装到 `BrowserManager` 后 Debug 包构建成功；MVP 后台策略保持不主动暂停 WebView |
| 2026-05-26 | `gradlew.bat assembleDebug` | 通过 | P2-05 补齐 `BrowserManager` 对 Cookie 策略、客户端绑定、下载监听、当前 URL、桌面 UA、JS 执行和浏览数据清理的分发接口后 Debug 包构建成功；剩余 URL 解析、请求拦截、JS 注入和设置存储按后续阶段拆分 |
| 2026-05-26 | `gradlew.bat assembleDebug` | 通过 | P2-06 回归验证前 Debug 包构建成功 |
| 2026-05-26 | `adb -s emulator-5554 install -r app/build/outputs/apk/debug/app-debug.apk` | 通过 | P2 版本安装到模拟器成功 |
| 2026-05-26 | `adb -s emulator-5554 shell am start -n com.example.videobrowser/.MainActivity` | 通过 | App 启动成功，首页、地址栏、WebView 和底部导航可见 |
| 2026-05-26 | 模拟器输入 `example.com` 并按回车 | 通过 | 地址栏同步为 `https://example.com/`，WebView 显示 Example Domain，后退按钮可用 |
| 2026-05-26 | 模拟器点击刷新、后退、前进、首页 | 通过 | 刷新保持当前页；后退回搜索首页且前进可用；前进回 Example Domain；首页回搜索首页并清空地址栏 |
| 2026-05-26 | 模拟器输入 `videobrowsertest` 并按回车 | 通过 | 地址栏同步为搜狗搜索结果 URL，页面搜索框显示 `videobrowsertest` |
| 2026-05-26 | 模拟器输入 `https://example.com` 并按回车 | 通过 | 完整 URL 可打开，地址栏同步为 `https://example.com/`，WebView 显示 Example Domain |
| 2026-05-26 | `gradlew.bat testDebugUnitTest` | 通过 | P3 新增 `UrlUtilsTest`，覆盖完整 URL、裸域名、关键词搜索、空输入和空白输入；解析逻辑位于 `utils/` 模块边界内 |
| 2026-05-26 | `gradlew.bat assembleDebug` | 通过 | P3 将地址栏解析迁移到 `UrlUtils` 后 Debug 包构建成功，仅保留既有 P4 全屏相关 deprecated 警告 |
| 2026-05-26 | `gradlew.bat assembleDebug` | 通过 | P4-01 创建 `ChromeClient` 入口后 Debug 包构建成功，仅保留既有 P4 全屏相关 deprecated 警告；新增代码位于 `browser/` 模块边界内 |
| 2026-05-26 | `gradlew.bat assembleDebug` | 通过 | P4-02 将 `onShowCustomView()` 进入全屏逻辑迁移到 `ChromeClient` 后 Debug 包构建成功；全屏相关 deprecated 警告已集中到 `browser/ChromeClient.kt` |
| 2026-05-26 | `gradlew.bat assembleDebug` | 通过 | P4-03 收紧 `onHideCustomView()` 退出全屏路径后 Debug 包构建成功；退出时先清空内部状态再恢复容器和通知回调，避免重复退出 |
| 2026-05-26 | `gradlew.bat assembleDebug` | 通过 | P4-04 补齐全屏容器可点击/可聚焦、Activity 硬件加速与方向配置，并强制 HTML5 视频显示原生 controls，修正视频只能播放但缺少控件入口的问题 |
| 2026-05-26 | `node --check app/src/main/assets/scripts/common.js` | 通过 | 视频浮层控制条脚本语法检查通过 |
| 2026-05-26 | `gradlew.bat assembleDebug` | 通过 | 修复视频控制体验：全屏强制横屏，HTML5 视频增加可拖动进度浮层和多 source 清晰度切换入口 |
| 2026-05-26 | `adb install -r -t app/build/intermediates/apk/debug/app-debug.apk` | 通过 | 最新 Debug 包已安装到真机 `2312DRA50C` 和 `emulator-5554` |
| 2026-05-27 | `gradlew.bat assembleDebug` | 通过 | P4-05 页面标题回调构建成功；标题分发仍位于 `browser/ChromeClient.kt`，UI 状态绑定位于 `MainActivity` |
| 2026-05-27 | `gradlew.bat assembleDebug` | 通过 | P4-06 页面加载进度回调构建成功；进度分发仍位于 `browser/ChromeClient.kt`，加载状态绑定位于 `MainActivity` |
| 2026-05-27 | `adb -s emulator-5554 install -r app/build/outputs/apk/debug/app-debug.apk` | 通过 | P4-07 验证前安装最新 Debug 包成功 |
| 2026-05-27 | `adb -s emulator-5554 shell am start -n com.example.videobrowser/.MainActivity` | 通过 | P4-07 验证前启动 App 成功 |
| 2026-05-27 | 本地 HTML5 video 测试页加载到 `emulator-5554` | 通过 | `http://10.0.2.2:8765/` 测试页可加载，UI dump 显示 `P4 HTML5 Video Test`、`video` 和 `Fullscreen` 按钮 |
| 2026-05-27 | 点击 HTML5 video `Fullscreen` | 通过 | UI dump 显示 `rotation="1"`，`fullscreenContainer` 覆盖全屏，顶部/底部浏览器栏隐藏，原生视频控制显示 `exit full screen` |
| 2026-05-27 | 全屏状态按返回键退出 | 通过 | UI dump 恢复 `rotation="0"`，顶部/底部浏览器栏与 WebView 正常显示，页面状态显示 `fullscreen exited` |
| 2026-05-27 | 再次进入全屏并点击播放器退出全屏按钮 | 通过 | UI dump 恢复竖屏，`fullscreenContainer` 不再显示，WebView 继续显示测试页 |
| 2026-05-27 | `node --check app/src/main/assets/scripts/common.js` | 通过 | 移除 HTML5 自定义播放器控制条后脚本语法检查通过 |
| 2026-05-27 | `gradlew.bat assembleDebug` | 通过 | 原生播放器 UI 复用规则落地后 Debug 包构建成功；手势层只调用播放器 API，不再绘制播放 / 暂停图标 |
| 2026-05-27 | `gradlew.bat testDebugUnitTest` | 通过 | 现有本地单元测试通过 |
| 2026-05-27 | `node --check app/src/main/assets/scripts/common.js` | 通过 | 返回/切换视频页时改为 suspend 而非 dispose 后脚本语法检查通过 |
| 2026-05-27 | `gradlew.bat assembleDebug` | 通过 | 导航前 suspend 页面视频、页面开始加载退出残留全屏状态后 Debug 包构建成功 |
| 2026-05-27 | `gradlew.bat testDebugUnitTest` | 通过 | 现有本地单元测试通过 |
| 2026-05-27 | `gradlew.bat assembleDebug` | 通过 | P5-01 创建并接入 `BrowserClient` 后 Debug 包构建成功；`WebViewClient` 回调入口位于 `browser/`，广告判断仍暂留 UI 回调，待 P5-02/P5-03 继续拆分 |
| 2026-05-27 | `gradlew.bat testDebugUnitTest` | 通过 | P5-01 抽取 `BrowserClient` 后现有本地单元测试通过 |
| 2026-05-27 | `gradlew.bat assembleDebug` | 通过 | P5-02 创建 `BrowserRequest` 并接入 `BrowserClient.shouldInterceptRequest()` 后 Debug 包构建成功 |
| 2026-05-27 | `gradlew.bat testDebugUnitTest` | 通过 | P5-02 请求数据结构接入后现有本地单元测试通过 |
| 2026-05-27 | `gradlew.bat assembleDebug` | 通过 | P5-03 创建 `AdBlockManager` 并迁移广告请求判断入口后 Debug 包构建成功 |
| 2026-05-27 | `gradlew.bat testDebugUnitTest` | 通过 | P5-03 广告判断入口迁移后现有本地单元测试通过 |
| 2026-05-27 | `emulator-5554` 加载本地 P5-03 广告拦截测试页 | 通过 | 测试页显示 `normal:loaded ad:blocked`；本地服务收到 `/`、`/normal.js` 和 `/favicon.ico`，未收到命中 `/pagead/` 的广告脚本请求 |
| 2026-05-27 | `gradlew.bat assembleDebug` | 通过 | P5-04 创建 `EmptyResponseFactory` 并迁移空响应构造后 Debug 包构建成功；空响应职责位于 `adblock/` 模块边界内 |
| 2026-05-27 | `gradlew.bat testDebugUnitTest` | 通过 | P5-04 空响应工厂接入后现有本地单元测试通过 |
| 2026-05-27 | `gradlew.bat testDebugUnitTest` | 通过 | P5-05 新增 `BuiltInAdBlockRulesTest`，覆盖内置广告域名、URL 片段、普通资源放行和域名子串误杀保护 |
| 2026-05-27 | `gradlew.bat assembleDebug` | 通过 | P5-05 内置 URL 黑名单拆分到 `adblock/BuiltInAdBlockRules.kt` 后 Debug 包构建成功 |
| 2026-05-27 | `gradlew.bat testDebugUnitTest` | 通过 | P5-06 接入 `AdBlockRequestInterceptor` 后现有本地单元测试通过 |
| 2026-05-27 | `gradlew.bat assembleDebug` | 通过 | P5-06 `shouldInterceptRequest()` 请求分发、广告判断和空响应生成链路接线后 Debug 包构建成功 |
| 2026-05-27 | `gradlew.bat testDebugUnitTest` | 通过 | P5-07 新增 `AdBlockRequestPolicyTest`，覆盖主文档命中黑名单仍放行、子资源命中拦截、关闭开关放行和非 HTTP 请求放行 |
| 2026-05-27 | `gradlew.bat assembleDebug` | 通过 | P5-07 主文档保护策略拆分到 `adblock/AdBlockRequestPolicy.kt` 后 Debug 包构建成功 |
| 2026-05-27 | `gradlew.bat testDebugUnitTest` | 通过 | P5-08 新增仪器测试前，现有本地单元测试通过 |
| 2026-05-27 | `gradlew.bat assembleDebugAndroidTest` | 通过 | P5-08 新增 `AdBlockRequestInterceptorInstrumentedTest` 后 Android 仪器测试 APK 编译成功 |
| 2026-05-27 | `gradlew.bat connectedDebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=com.example.videobrowser.adblock.AdBlockRequestInterceptorInstrumentedTest` | 通过 | P5-08 目标仪器测试在真机 `2312DRA50C` 和 `emulator-5554` 通过；命中 `doubleclick.net/pagead` 的子资源返回 204、`No Content`、`text/plain`、`utf-8` 和空 body |
| 2026-05-27 | `gradlew.bat assembleDebug` | 通过 | P5-08 空响应验证完成后 Debug 包构建成功 |

---

## 20. 下一次开发建议

下一次开发在开发工程师测试并确认 G2-03 后，继续 G2 规则编译与索引：

```text
G2-04 为 URL contains 规则建立关键词索引。
```

推荐改动范围：

- 在 `RequestRuleIndex` 现有 fallback 基础上只先处理 `RuleType.URL_CONTAINS` 规则。
- 为 URL contains 规则建立关键词候选索引，保持白名单优先于阻断规则。
- 继续保留 URL pattern 和无法提取稳定关键词的规则 fallback，确保索引结果和当前 `RuleEngine` 匹配结果一致。
- 继续复用 G1 的 `RequestContext` 和资源类型限制，避免索引后丢失上下文语义。

暂时不要做：

- 不要先做远程订阅下载、增量更新或复杂资源类型选项。
- 不要一次性启用完整 EasyList / AdGuard 语法；未知或高风险语法继续默认跳过。
- 不要重做功能中心 UI；优先让解析结果进入现有日志和规则加载链路。
- 不要再把新业务逻辑直接加入 `MainActivity`；如发现需要新增职责，先创建对应模块文件。

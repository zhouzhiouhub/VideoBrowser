package com.example.videobrowser.browser

import android.webkit.WebView

class BrowserManager(
    private val webView: WebView
) {
    fun setup() {
        // WebView settings migration happens in P2-02.
    }
}
